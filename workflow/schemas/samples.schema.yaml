$schema: "http://json-schema.org/draft-06/schema#"

description: an entry in the sample sheet
properties:
  sample_name:
    type: string
    description: sample name/identifier
  condition:
    type: string
    description: any metadata condition associated with the sample

required:
  - sample_name
  - condition
